import { createTheme } from "@mui/material/styles";

const Theme = createTheme({
  palette: {
    primary: {
      main: "#2f2f33",
      dark: "#212121",
      text: "#ff0000",
    },
    secondary: {
      main: "#f0f2f5",
    },
  },
});

export default Theme;
